# paulocsanz/tikv fork — agent standing orders

## Merge policy (2026-08-08)

- **Auto-merge** harness / DST / tooling PRs into `fork/master` as soon as green. Do **not** ask.
- **Do not auto-merge** production **bug + proof test** PRs intended for **upstream** `tikv/tikv` — leave OPEN for human review and upstream PR.
- **Never** open/merge upstream without explicit order.

Detail lives in monorepo `tikv-dst/WORKFLOW.md`.
